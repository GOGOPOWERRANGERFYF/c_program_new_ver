笔记：

学习CSS的核心，重点，关键：真正理解元素类型，布局和格式上下文。
正常文档流属于布局不属于格式上下文。
布局和格式上下文之间的区别和关系要厘清。
格式化上下文由特定的布局属性（如 float, display, overflow 等）触发

块级元素默认布局为正常文档流

颜色：
rgb 值越接近0，越接近黑色，越接近255(10)或ff(16)越接近原色。
rgb (255, 99, 0) 或 #ff6300
还是有alpha rgba (255, 99, 0, 0.5)
opacity: 0.5;属性调整的是整个元素的透明度


想象某个长度半径的圆顶住矩形的某个角

<html>为块级元素，默认布局正常文档流;
<body>为块级元素，默认正常文档流,非任何格式上下文。
某元素激活格式上下文,为该元素包含区域的独立布局。
格式上下文有：

行内元素不能使用块元素作为子元素

